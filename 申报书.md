# MoonBit 黑客松项目申报书：moonbit-freight-calculator

**项目目标**：建设一个 MoonBit 多规则运费计算与计费审计引擎，服务快递、仓配、跨区配送等场景。项目以“规则可组合、结果可解释、测试可复现”为核心，把首重续重、体积重、分区倍率、偏远费、超重/超长附加费、燃油附加费、保价费和最低收费统一抽象为类型安全的计费模型，输出可用于账单、客服解释和运营复核的明细。

**选题依据**：报名准备阶段检索 mooncakes.io 的 freight、tariff、pricing、logistics 等关键词，未发现成熟且功能高度重合的运费规则引擎。MoonBit 生态已有 JSONPath、正则、decimal 等通用基础库，本项目定位在物流业务规则层，既能展示 MoonBit 静态类型、模式匹配、快照测试和多后端编译优势，也能补充真实业务应用样例。

**核心实现范围**：仓库包含纯 MoonBit 库与 CLI 示例。公共模型包括 `Money`、`ParcelDim`、`BillingRule`、`ZoneRate`、`SurchargePolicy`、`Quote`、`ChargeLine` 和 `ManifestQuote`；核心算法包括实际重/体积重取大、首重续重阶梯计费、区域倍率和偏远费计算、超重超长附加费、燃油和保价比例费、最低收费补齐、批量 manifest 汇总，以及 `validate_rule` 规则体检。金额使用整型分计价，避免浮点误差。

**工程计划与质量保障**：项目采用标准 `moon.mod`/`moon.pkg` 布局，源码拆分为类型、金额、包裹、规则、引擎、校验、格式化、样例和 CLI 等多个文件，降低维护成本。测试覆盖金额格式、体积重、区域计费、附加费、批量汇总和规则校验。CI 参考官方社区模板，在 Linux、macOS、Windows 上执行 `moon fmt --check`、`moon check --deny-warn`、`moon info`、`moon test --deny-warn` 和 native 后端测试；由于当前本地 `moon info` 子命令不支持 `--deny-warn`，仓库以 `moon check --deny-warn` 作为警告闸门，并保留 `moon info` 生成接口文件。

**开源与后续扩展**：仓库使用 MIT License，README 提供项目背景、快速开始、结构说明、验证命令和路线图。后续将扩展 JSON/CSV 费率表导入、客户等级折扣、时效 SLA 规则、国际分区模板和 mooncakes 发布，使项目从黑客松样例成长为可复用的 MoonBit 物流计费组件。
